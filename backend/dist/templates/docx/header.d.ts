/**
 * Header DOCX Template
 * Full-width dark header band with name/contact, then single-column body.
 * Used for all header-* templates (header-dark, header-dark-banner, etc.).
 */
import { PdfResumeData, PdfTheme, PdfTranslations } from '../../types/pdf';
export declare const renderDocxHeader: (data: PdfResumeData, theme: PdfTheme, translations?: PdfTranslations, locale?: string) => string;
//# sourceMappingURL=header.d.ts.map